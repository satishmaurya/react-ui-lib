import React from 'react';
import PropTypes from 'prop-types';

const EventGroupTypes = () => <>Event</>;

EventGroupTypes.propTypes = {
  
};


EventGroupTypes.defaultProps = {
   
};

export default EventGroupTypes ;
