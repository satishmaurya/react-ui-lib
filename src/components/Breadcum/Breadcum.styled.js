import Styled,{css} from "styled-components";
import * as variable from "../../variable";
import Breadcrumb from 'react-bootstrap/Breadcrumb'

const StyledBreadcum = Styled(Breadcrumb)`
    overflow:hidden;
   
`;

export default StyledBreadcum;