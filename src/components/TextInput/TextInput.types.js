import React from 'react';
import PropTypes from 'prop-types';

const TextInputTypes = () => <>TextInput</>;

TextInputTypes.propTypes = {
  
};

TextInputTypes.defaultProps = {
  
};

export default TextInputTypes ;
